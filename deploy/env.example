# MCP-Hound Environment Configuration
# Copy to /etc/mcp-hound/env and adjust as needed

# Hound server URL
HOUND_URL=http://localhost:6080

# Hound request timeout (ms)
HOUND_TIMEOUT=30000

# Gitea server URL (for file context deep links)
GITEA_URL=https://gitea.example.com

# Gitea API token (for private repositories)
# Generate at: <your-gitea>/user/settings/applications
#GITEA_TOKEN=your_token_here

# Gitea request timeout (ms)
GITEA_TIMEOUT=10000

# MCP server port (when running in HTTP mode)
MCP_PORT=3100
