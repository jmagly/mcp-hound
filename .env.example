# MCP-Hound Configuration

# Hound server URL (required)
HOUND_URL=http://localhost:6080

# Request timeout in milliseconds
HOUND_TIMEOUT=30000

#=============================================================================
# Git Provider Configuration
# Configure ONE of the following: Gitea OR GitHub
# Priority: GITEA_URL > GITHUB_URL
#=============================================================================

# Option 1: Gitea
# GITEA_URL=https://gitea.example.com
# GITEA_TOKEN=your_gitea_token
# GITEA_TIMEOUT=10000

# Option 2: GitHub (public or Enterprise)
# GITHUB_URL=https://github.com                    # Optional, defaults to github.com
# GITHUB_TOKEN=your_github_personal_access_token   # Required for private repos
# GITHUB_TIMEOUT=10000

#=============================================================================
# HTTP Mode Configuration
#=============================================================================

# Port to listen on
MCP_PORT=3000

# CORS allowed origin (leave empty for permissive mode in development)
# Set to your client origin in production, e.g., https://claude.ai
CORS_ALLOWED_ORIGIN=

# Path to store client credentials
MCP_CREDENTIALS_FILE=/etc/mcp-hound/clients.json

#=============================================================================
# Auto-Indexing (Gitea webhooks)
#=============================================================================

# Path to Hound config directory
HOUND_CONFIG_DIR=

# Webhook signature verification secret
HOUND_WEBHOOK_SECRET=
